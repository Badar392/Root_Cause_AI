streamlit>=1.32
pandas>=2.0
numpy>=1.24
plotly>=5.18
groq>=0.9
python-dotenv>=1.0
pdfplumber>=0.10
python-docx>=1.1
reportlab>=4.0
Pillow>=10.0
requests>=2.31

# Optional, only needed for specific Data Connectors (app degrades gracefully
# with a clear message if these aren't installed):
# sqlalchemy>=2.0          # non-SQLite database connector (Postgres, MySQL, etc.)
# google-analytics-data>=0.18   # GA4 connector
# google-auth>=2.23             # GA4 connector

